interface:
  display_name: "Callbell Core"
  short_description: "Diagnose, Statusline, Ping oder Planungssystem verwalten"
  default_prompt: "Nutze $callbell-core doctor, um den Callbell-Projektzustand ohne Änderungen zu prüfen."

policy:
  allow_implicit_invocation: false
