interface:
  display_name: "Callbell Dev Review"
  short_description: "Code gezielt auf unnötige Komplexität prüfen"
  default_prompt: "Nutze $callbell-dev-review, um diesen Code gezielt auf unnötige Komplexität zu prüfen."

policy:
  allow_implicit_invocation: false
